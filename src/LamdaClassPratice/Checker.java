package LamdaClassPratice;

interface Checker {
   // make a functional interface (interface = IChecker method = check) to check an int even or not,
    //if it is even return true/false
   boolean IChecker(int n);
}
